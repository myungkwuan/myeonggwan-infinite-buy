# 로컬은 SQLite로 바로 테스트 가능. Railway 배포 시 PostgreSQL URL 자동 주입됨.
DATABASE_URL=sqlite:///./local.db
# 예) DATABASE_URL=postgresql://user:pass@host:5432/dbname

# 프론트 주소(쉼표 구분). 개발 중엔 * 허용.
CORS_ORIGINS=*
# 예) CORS_ORIGINS=https://your-frontend.vercel.app
